import shap
import pandas as pd
import joblib

def explain_prediction(input_data):
    return {
        "message": "This legacy explainability helper is deprecated. Use the /explain API endpoint instead."
    }